# sub() and gsub()

objA <- "Mercury/Venus/Earth/Mars"
objB <- sub('/','-',objA)   # replace first / with -
objC <- sub('/','-',objB)   # replace second / with -
objD <- sub('/','-',objC)   # replace third / with -
objE <- sub('/','-',objD)   # replace fourth / with -
